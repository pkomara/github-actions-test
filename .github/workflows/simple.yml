name : shell commands
on : 
  push :
    branches:
      - master
  # repository_dispatch : 
  #   types : [build]
  # schedule :
  #   - cron : "0/5 * * * *" 

jobs :
  run-shell-command :
    runs-on : ubuntu-latest
    env:
      environment : stage
    steps :
      # - name : payload
      #   run : echo ${{ github.event.client_payload.env}}
      # - name : echo a string
      #   run : echo "Run git actions"
      # - name : mutli line
      #   run : |
      #     node -v 
      #     npm -v  
      - name : check 
        run : |
          echo "${{ env.environment}}"
          if [env.environment === 'stage' ]
          then
            echo "in if"
            echo "https://pureengage.jfrog.io/pureengage/helm-${{env.environment}}ing"
          else
            echo "in else"
            echo "https://pureengage.jfrog.io/pureengage/helm-${{env.environment}}"
          fi